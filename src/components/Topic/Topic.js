import React from 'react';

const Topic = ({loadTopic}) => {
    // const [id , logo] 
    console.log(loadTopic);
    return (
        <div>
            
            {/* <h1>Name: {logo}</h1> */}
        </div>
    );
};

export default Topic;